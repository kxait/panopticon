procs:
  -
    name: ls
    cmd: ls
  -
    name: system log
    cmd: tail
    args:
      - '-f'
      - /var/log/system.log
  -
    name: funky
    cmd: sh
    args:
      - -c
      - while true; do echo $(date -u); sleep 0.1; done
